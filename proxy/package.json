{
  "name": "@dsh-mobile/remote",
  "version": "1.1.1",
  "private": true,
  "description": "dsh-remote: token-guard reverse proxy exposing a loopback dsh web host to the LAN/mobile app",
  "type": "module",
  "bin": {
    "dsh-remote": "./dsh-remote.mjs"
  },
  "scripts": {
    "start": "node dsh-remote.mjs",
    "test:e2e": "playwright test",
    "test:e2e:chrome": "playwright test --project=chromium",
    "test:e2e:safari": "playwright test --project=webkit"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
