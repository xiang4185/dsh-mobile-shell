{
  "name": "@dsh-mobile/app",
  "version": "1.1.1",
  "private": true,
  "description": "dsh-mobile: Capacitor shell connecting to a self-hosted DeepSeek Harness (dsh web) host",
  "type": "module",
  "scripts": {
    "sync": "cap sync && node ../scripts/fix-spm-vendor.mjs",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android": "cd android && ./gradlew assembleDebug",
    "build:ios": "npm run sync && xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath ios/build CODE_SIGNING_ALLOWED=NO build",
    "typecheck": "tsc --noEmit capacitor.config.ts --module nodenext --moduleResolution nodenext --target es2022 --skipLibCheck"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/keyboard": "8.0.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "typescript": "7.0.2"
  },
  "overrides": {
    "xcode": {
      "uuid": "11.1.1"
    }
  }
}
