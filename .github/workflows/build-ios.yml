name: build-ios

# Fork trigger: keep this workflow indexed and runnable on GitHub-hosted macOS.

on:
  push:
    branches: [main]
    paths: ['app/ios/**', 'app/www/**', 'app/capacitor.config.ts', '.github/workflows/build-ios.yml']
  pull_request:
    paths: ['app/ios/**', 'app/www/**', 'app/capacitor.config.ts', '.github/workflows/build-ios.yml']
  workflow_call: {}
  workflow_dispatch: {}

jobs:
  ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      # No network beyond npm: Capacitor's SPM binaries are vendored under
      # app/ios/vendor/ (sha256-verified, ADR-0005), so xcodebuild never
      # touches github.com release downloads.
      - name: Install JS dependencies
        run: npm ci --prefix app

      # cap sync copies www/ into ios/App/App/public (gitignored build output)
      # and writes capacitor.config.json. It also regenerates
      # CapApp-SPM/Package.swift — reverting the vendored local-path
      # dependency (ADR-0005), so re-apply it right after.
      - name: Capacitor sync
        run: |
          npx cap sync ios
          node ../scripts/fix-spm-vendor.mjs
        working-directory: app

      - name: Build for iOS Simulator
        run: >-
          xcodebuild -project app/ios/App/App.xcodeproj -scheme App
          -configuration Debug -sdk iphonesimulator
          -destination 'generic/platform=iOS Simulator'
          -derivedDataPath app/ios/build
          CODE_SIGNING_ALLOWED=NO build

      - name: Zip simulator app
        run: ditto -c -k --sequesterRsrc App.app "$GITHUB_WORKSPACE/DSH-simulator.app.zip"
        working-directory: app/ios/build/Build/Products/Debug-iphonesimulator

      - uses: actions/upload-artifact@v4
        with:
          name: dsh-mobile-ios-simulator-app
          path: DSH-simulator.app.zip

      - name: Build for iOS Device (unsigned)
        run: >-
          xcodebuild -project app/ios/App/App.xcodeproj -scheme App
          -configuration Debug -sdk iphoneos
          -destination 'generic/platform=iOS'
          -derivedDataPath app/ios/build-device
          CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build

      - name: Package unsigned device IPA
        run: |
          rm -rf device-payload
          mkdir -p device-payload/Payload
          cp -R app/ios/build-device/Build/Products/Debug-iphoneos/App.app device-payload/Payload/
          cd device-payload
          zip -qry "$GITHUB_WORKSPACE/DSH-unsigned.ipa" Payload

      - uses: actions/upload-artifact@v4
        with:
          name: dsh-mobile-ios-unsigned-device-ipa
          path: DSH-unsigned.ipa
